s = "somnath"
# s[0] = "c"    #we can't do that with string, because string is immutable
print(s)

a = ['a', 'b', 'c']
a[0] = 'd'  # with list we can do this, because list is mutable
print(a)

# tuples
b = ('a', 'b', 'c', 123, None, True)
print(b)
# b[0] = 'e'  #with tuple we can't do this
# print(b)

'''
# Tuple operations

Positive and negative indexing -> like list
Slicing -> like list
Iterating through a tuple -> like list
Tuple functions -> like list
'''
